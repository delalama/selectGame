import { XpAnimationDirectiveDirective } from './xp-animation-directive.directive';

describe('XpAnimationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new XpAnimationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
